# Demo

From any project directory:

```text
/ber fix agent gave a vague update -> agent gives exact command output and next action
/ber remember design human-facing software around the shortest path to outcome
/ber report
```

Expected result: the OpenClaw agent responds with the captured lesson or report. Helper paths, target files, and evidence files stay in the background.
